name: Okteto Deployment Pedro

on:
  push:
    branches: [ master ]

jobs:
  test:
    uses: MUISCDGTI/Usuarios/.github/workflows/tests.yml@master
    
  deploy:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2


      - name: Okteto Login
        uses: okteto/login@latest
        with:
          # Your okteto API token (changeit) ${{ secrets.OKTETO_TOKEN_PEDRO }}
          token: 0SElSyHxMULypfGhuIwD35GZivLU5w0JWIusfEcF1c3km362
          
      - name: Okteto Pipeline
        uses: okteto/pipeline@latest
        with:
          # The name of the pipeline
          name: usuarios
          # If present, the namespace scope for this action. If not specified, it will use the current context
          namespace: fis-gfpedro
